
main

  title = {text}
  subtitle = {text}

  logo
    image
    border
       top, bottom, left, right
  
  

  legend
    border
       top, bottom, left, right
    @label
      border
        top, bottom, left, right
      @color
      axis_color  
    title = {text}
    description = {text}
    ?box
    

    axis
      radial
      polar
      x,y,z? 
        label
        ?arrow
        ?zero   axis visible
        ?log
        ?invert
        ?visible
          
      tick
        start
        end
        division
        label_skip
        @label
          color
          font
          style
        @special
          text
          color
          pos
        
      subtick
        division
  
      grid
        special
        top
        visible
        start
        end
        skip
        color


plot

  data
    properties = [x, y, size, color, shape]
    
  position
  size
  color
  shape
  axis
    name
    unit
    min
    max
    log =?
    polar ?
    stack ?
    smooth = +
    join ?

box


data
   set:

color



border 
    left
    right
    top
    bottom
    
    
    

um mich herum

    margin-top
    margin-right
    margin-bottom
    margin-left

    border: dotted, dashed, solid, 

    padding-top
    padding-right
    padding-bottom
    padding-left
    
    content


domain-axis
scaling = linear/log  
shape = lidear / radial

data
  set
  domain
  


{text}
    text
    color
    font
    border
       top, bottom, left, right
